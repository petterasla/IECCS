WOS XML XPath

Everything is stored in list
Use root.findall(path).
Example: root.findall(".REC/UID") to find UID list
and use root.findall(".REC/UID")[0].text to extract this value. Use for-loop for more than one value

##### UID #####
.REC/UID

Value:
- WOS Unique ID
(".REC/UID")[0].text


##### Dynamic data #####
.REC/dynamic_data

Values:
- Cluster related: type and value
("./REC/dynamic_data/cluster_related/identifiers/identifier")[0].attrib

- Citation related: coll_id and local_count
("./REC/dynamic_data/citation_related/tc_list/silo_tc")[0].attrib


##### Static data #################
.REC/static_data

    ######## Item ########
    /item

    values:
    - Keyword plus: List of keywords (plus)
    ("/item/keywords_plus/")

    - Some id value
    ("/item/ids")[0].text

    - Bib id
    ("/item/bib_id")[0].text

    ####### Fullrecord metadata #########
    /static_data

    values:
    - Languages
    ("/static_data/fullrecord_metadata/languages/language")[0].text

    - Normalized(?) language: i.e English
    ("/static_data/fullrecord_metadata/normalized_languages/language")[0].text

    - Normalized(?) document type: i.e Article
    ("/static_data/fullrecord_metadata/normalized_doctypes/doctype")[0].text

    - References: i.e: 50
    ("/static_data/fullrecord_metadata/refs")[0].attrib['count']

    - Number of organizations contributing
    /static_data/fullrecord_metadata/addresses/")

    - List with address info like street, city, country
    /static_data/fullrecord_metadata/addresses/address_name/address_spec/")